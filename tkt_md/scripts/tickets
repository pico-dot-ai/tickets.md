#!/usr/bin/env python3
import sys
from pathlib import Path

# Ensure the package in ../tickets is importable when run from repo root.
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from tickets.cli import main

if __name__ == "__main__":
    sys.exit(main())
